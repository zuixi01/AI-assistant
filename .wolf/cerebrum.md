# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-06-09

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->

- **UI redesign workflow:** User prefers receiving a layout/beautification plan first before implementation changes.
- **Planning output:** When asking for plans, user prefers directly executable development task lists rather than only high-level phase descriptions.

## Key Learnings

- **Project:** ai-commerce-assistant
- **Frontend port:** Next.js Web app runs on port 3100 via `apps/web/package.json`; API runs on port 4000. Port 3000 may be occupied by OpenSwarm.
- **API proxy:** `next.config.ts` rewrites `/api/*` to `http://localhost:4000/api/*`
- **Seed password:** Admin demo password is `admin123456` (not `admin123`)
- **PrismaClient in scripts:** Must set DATABASE_URL explicitly or run from packages/database dir; root pnpm workspace resolves @prisma/client differently
- **Node.js $ escaping:** In bash `-e` inline scripts, `$disconnect()` gets mangled; write .js files instead
- **i18n:** 4 locale files (zh/en/ja/fr) must be updated in sync; zh.ts defines the Translations type
- **Frontend builds:** `pnpm --filter @ai-assistant/web build` succeeds with 13 routes
- **Stale .next cache:** After editing source files, if runtime error "Cannot find module './NNN.js'" occurs, delete `.next/` and restart dev server
- **Next dev/build cache interaction:** If `pnpm build` runs while `next dev` is still serving `apps/web`, local route checks may hit stale React Client Manifest errors. Stop Web dev, verify/remove `apps/web/.next`, then restart `pnpm --filter @ai-assistant/web dev`.
- **Dev script port:** `pnpm --filter @ai-assistant/web dev` runs `next dev -p 3100`; use `http://localhost:3100` for local Web checks.
- **DesignQC port:** `openwolf designqc` may auto-detect an unrelated localhost server such as port 3001; for this Web app force `openwolf designqc --url http://localhost:3100 ...` after confirming the 3100 dev server is serving the current workspace.
- **Prisma regenerate locked DLL:** If `prisma generate` fails with EPERM on query_engine-windows.dll.node, delete `node_modules/.pnpm/@prisma+client*/node_modules/.prisma/client` directory first, then regenerate
- **Prisma schema relations:** When adding a new model with tenantId, must add `tenant Tenant @relation(...)` field AND update Tenant model's relation array; otherwise validation fails
- **RAG pipeline architecture:** knowledge controller → knowledge service → pipeline service → chunker factory → embedding service → raw SQL insert. Retrieval uses semantic+keyword hybrid with RerankService for multi-factor scoring
- **RAG vector optimization analysis:** Current retrieval stores `vector(1536)` in Postgres and orders by `embedding <=> query` without an ANN vector index. turbovec is best evaluated as an optional Python sidecar/local cache accelerator with pgvector as source of truth and fallback; UUID chunk IDs need a uint64 mapping for turbovec `IdMapIndex`.
- **RAG vector accelerator implementation:** turbovec is now feature-flagged through `VECTOR_SEARCH_PROVIDER=turbovec` and `TURBOVEC_ENABLED=true`; pgvector remains fallback/source of truth. Retrieval rehydrates sidecar hits through Postgres and validates active tenant/source rows. Chunk ingestion paths call `VectorStoreService.syncChunksToAccelerator` after DB insert.
- **ESLint config:** Root `eslint.config.mjs` only registers `@typescript-eslint`; do not add `// eslint-disable-next-line react-hooks/exhaustive-deps` unless the react-hooks plugin is configured, or lint will fail with "Definition for rule ... was not found".
- **Admin Web auth implementation:** Admin login now sets `admin_token` as an httpOnly cookie and returns only admin profile data; admin pages use `apps/web/src/lib/api.ts` with `credentials: 'include'` and no longer store/send browser Bearer tokens.
- **Knowledge indexing reliability:** Knowledge source upload/create/reindex now enqueues BullMQ `knowledge-index:index-source` jobs instead of kicking off direct fire-and-forget pipeline work; Redis defaults align to docker-compose port 6380.
- **Production verification scripts:** Root scripts now have executable smoke/eval targets for integration, E2E auth contract, RAG fixture validation, AI safety fixture validation, and mock webhook checks. Web lint uses `apps/web/eslint.config.mjs`; root lint uses a generic TypeScript ESLint config.
- **Docker Desktop on Windows:** Sometimes Docker API returns 500 but containers may still be running; check with `netstat -ano | findstr PORT` to verify ports are actually listening
- **Prisma BigInt serialization:** Prisma `@db.BigInt` fields return JS `BigInt` which `JSON.stringify` cannot serialize. Must add `BigInt.prototype.toJSON = function() { return Number(this); }` in main.ts. Also TypeScript type for BigInt fields must be `bigint`, not `number`
- **Architecture snapshot:** Project is a multi-tenant AI ecommerce customer service assistant. Core flow is chat -> intent/query rewrite -> hybrid RAG retrieval/rerank -> LLM response -> messages/retrieval logs/unknown questions; product import syncs product knowledge into the RAG corpus.
- **Xiaohongshu integration:** XHS third-party customer service API integrated at `apps/api/src/integrations/xiaohongshu/`. Uses AES-128-CBC encryption (`{base64_iv}~split~{base64_data}` format), webhook-based message push (not polling), `Access-Token` header auth. Webhook endpoints at `/api/webhooks/xiaohongshu/*` (no JWT), admin endpoints at `/api/admin/integrations/xiaohongshu/*` (JWT). AI auto-reply flow: webhook → decrypt → store XhsMessage → find/create Conversation → ChatService.chat() → encrypt reply → XHS sendMessage API. Models: `XhsAccount` (tenant-bound accounts), `XhsMessage` (all messages with conversation link). Env vars: `XHS_CRYPTO_KEY`, `XHS_APP_ID`, `XHS_ACCESS_TOKEN`.
- **Xiaohongshu binding flow:** The 2026 third-party customer service document uses Juguang account binding, not generic OAuth. QR configuration must point to `https://ad.xiaohongshu.com/api/leona/three/im/account/add?appId=...&token=...`, where `token` decrypts to at least `account_code` plus tenant identity. Incoming public paths must include `/api/open/im/send`, `/api/open/im/third/bind_account`, `/api/open/im/third/unbind_account`, and `/api/open/im/auth/bind_user/event`. Outgoing TEXT replies must encrypt JSON `{"text":"..."}`, not raw text.
- **Prisma db push vs migrate:** When pre-existing migrations fail on shadow database (e.g., turbovec migration referencing columns that exist in prod but not in replay), use `prisma db push` to sync schema directly instead of `prisma migrate dev`.
- **Current optimization hotspots:** Many admin detail/update/delete services still query by `id` only instead of `{ id, tenantId }`; `PythonSidecarClient` is registered but not wired into `DocumentParserService`; root test scripts reference integration/e2e/eval files that are not present under `tests/`.
- **Workspace git state:** `D:\智能客服助手` currently has no `.git` directory, so do not rely on git status/diff in this workspace unless a repo root is found elsewhere.
- **Customer service workflow:** ChatService now uses conversation workflow statuses `open`, `pending_human`, `needs_review`, and `closed`. High-risk after-sale/human-service turns force `transferred_to_human` and `pending_human`; low-confidence knowledge misses become `needs_review` + UnknownQuestion; low-confidence casual dialogue stays `open` and does not pollute unknown-question analytics.
- **Public chat access model:** Public conversations now return a one-time raw session token and store only a SHA-256 `publicTokenHash`; public conversation/chat/message/lead access must validate `x-conversation-token` or an equivalent body token before using the conversation.
- **Chat polling model:** The tenant chat page persists the public conversation token per tenant slug and polls messages incrementally with an `after` timestamp cursor, merging persisted messages with optimistic local messages.
- **Chat quota enforcement:** ChatService checks `TenantsService.checkLimit(tenantId, 'messages')` before public chat/stream turns and blocks before writes when remaining quota cannot cover a user+assistant turn.
- **RAG eval depth:** `tests/evals/run-rag-eval.ts` is live-aware: auto/live mode can log in, create a temporary FAQ source, wait for indexing, exercise retrieval and RAG answer endpoints, then clean up; fixture mode still validates schema when live API is unavailable.
- **Knowledge admin routes:** KnowledgeController is mounted at `/api/admin/knowledge`; live evals, smoke scripts, and admin clients must target `/admin/knowledge/*`, not `/knowledge/*`.
- **Live verification environment:** Before running live API/RAG checks, verify port 4000 belongs to this project; in this session it was occupied by a Node process from `D:\奶茶小程序开发`. Use a free API_PORT plus `RAG_EVAL_API_URL`, and ensure Docker Desktop/Postgres/Redis are running.
- **Current audit status:** On 2026-06-14, `pnpm validate:before-commit` fails because unit/integration tests reference missing `apps/api/src/products`, `apps/api/src/orders`, and `apps/api/src/integrations/doudian` source files. Current API build and Web/embed builds pass, but product/order/Doudian feature contracts are broken or stale.
- **Admin API contract gaps:** Web pages currently call several mismatched endpoints/methods: knowledge enhancements page targets `/api/admin/knowledge/*` while backend mounts `/api/admin/knowledge/enhancements/*`; Juguang edit uses POST while backend expects PATCH; workspace stats response fields do not match frontend expectations.
- **Workspace tenant isolation:** `WorkspaceService.assignConversation`, `unassignConversation`, `updateStatus`, and `sendManualReply` must verify `{ id, tenantId }` via `findTenantConversationOrThrow` before writing.

- **Premium Web UI system:** The Web UI now uses `apps/web/src/components/ui/design-system.tsx` plus `apps/web/src/app/globals.css` `premium-*` classes for page shells, glass panels, stat cards, status pills, progress bars, action buttons, animated chat bubbles, and premium tables. Prefer these primitives/classes for future page work instead of ad hoc gray cards.
- **Conversation records UI target:** The admin “会议记录/会话记录” experience lives in `apps/web/src/app/admin/conversations/page.tsx` for the list view and `apps/web/src/app/admin/conversations/[id]/page.tsx` for the split detail workbench; redesigns should preserve the existing API calls and status/action handlers while changing layout/styles.
- **Authenticated admin visual QA:** When OpenWolf DesignQC captures `/admin/*` login, demo auth can be verified through `admin@demo.com` / `admin123456`, then Playwright CLI can set `admin_token` and capture real admin pages. On this Windows machine, Playwright CLI must open with `--browser msedge` because Chrome is not installed at the default path.
- **DesignQC port:** For this workspace, always run OpenWolf design capture with `--url http://localhost:3100`; auto-detection may find an unrelated OpenSwarm service on port 3000 and capture the wrong app.
- **Protected admin visual QA:** `openwolf designqc` does not inject admin cookies, so protected `/admin/*` captures can show the login page even when the route builds. Verify auth separately with `POST /api/auth/login` + `GET /api/auth/me`; use an authenticated browser workflow when actual admin screenshots are required.
- **Architecture simplification hotspots:** Current backend complexity is concentrated less in file count and more in overlapping responsibilities: `ChatService` is the main orchestration module for chat, retrieval, status transitions, unknown-question logging, product recommendation extraction, and message persistence; `CustomerServiceRAGService` looks like a main RAG answer module but currently returns knowledge context while `ChatService` performs the LLM call.
- **Platform chat persistence contract:** `PlatformRouterService` must save the inbound platform user message once, then call `ChatService.generateReplyForPersistedTurn()` and save one adapted assistant message. Do not call `ChatService.chat()` from platform router paths because `chat()` owns public chat message persistence.
- **Knowledge module public interface:** `KnowledgeModule` should export only `KnowledgeService` and `RetrievalService`; parser/chunker/pipeline/vector/sidecar/index queue providers are internal implementation details. `KnowledgeEnhancementsModule` is imported through `KnowledgeModule`, not directly by `AppModule`.
- **Meiqia-style AI客服 verification:** Focused customer-service regression should run `chat-service-workflow`, `retrieval-model-config`, `workspace-tenant-isolation`, `message-adapter`, and `platform-router`. In this pass those 5 suites passed with 12 tests.
- **Platform reply adaptation:** `MessageAdapterService` normalizes plain-text channel replies after markdown stripping and concise filler removal, including duplicate Chinese punctuation cleanup.
- **Product/order/Doudian service restoration:** `ProductsService`, `OrdersService`, and `DoudianService` are live again under `apps/api/src/`. Doudian product sync must call `ProductsService.syncProductKnowledge` after upserting each product so AI客服 RAG can answer product questions from fresh product knowledge.
- **Module boundary tests:** Keep `tests/unit/module-boundaries.test.ts` imports lightweight. Do not import `AppModule` in KnowledgeModule-only assertions; full AppModule imports can exceed Vitest's per-test timeout under concurrent full unit runs.
- **Tenant model config runtime:** Admin model settings stored in `Tenant.config.modelConfig` must be used at runtime by `ChatService` and `RetrievalService`; provider factories should create per-request OpenAI-compatible providers from the tenant config instead of relying only on `.env`.
- **Embedding dimension fallback:** The demo tenant currently uses an embedding provider/model that returns 1024-dimensional vectors while `knowledge_chunks.embedding` is `vector(1536)`. Retrieval must detect this mismatch and skip pgvector semantic search, falling back to keyword retrieval so knowledge-backed chat still answers instead of returning a send failure or empty RAG result.
- **Live chat runtime dependencies:** Final public chat verification needs Docker Desktop running with `docker compose up -d postgres redis`, API listening on 4000, and Web dev listening on 3100. If health returns `000`, check `.wolf/api-chat-fix.err.log` for Postgres/Redis connection refusal before debugging code.
- **Public chat vs embed widget:** `apps/web/src/app/chat/[tenantSlug]/page.tsx` and `apps/embed-widget/src/ChatWidget.tsx` now share the same token-gated public chat lifecycle via `packages/shared/src/public-chat-client.ts`: create conversation, persist public token, poll `/messages` with `after`, and send turns through `/api/chat`.
- **Planning skill path on this machine:** `planning-with-files-zh` templates/scripts are under `C:/Users/29408/.agents/skills/planning-with-files-zh/`, not `.claude/skills/`; use the `.agents` path when reading templates in this workspace environment.
- **Platform conversation hardening:** `Conversation` now stores nullable `platformUserId` / `platformAccountId`; `PlatformRouterService` must find/reuse platform conversations by these identifiers instead of “latest open conversation by channel”.
- **Pending DB migration symptom:** If admin dashboard loads `analytics` but `/api/admin/conversations` and `/api/admin/leads` both return 500 with Prisma `P2022` on `conversations.platform_user_id`, the database is missing `packages/database/prisma/migrations/20260615003000_platform_conversation_keys`; apply the pending migration before changing service code.
- **Root Prisma guard env loading:** Root-level `tsx` scripts that invoke Prisma CLI must load `.env` themselves in this workspace; otherwise `pnpm db:ensure` can fail with `P1012 Environment variable not found: DATABASE_URL` even though `apps/api/.env` exists.
- **Conversation detail assignment contract:** `apps/web/src/app/admin/conversations/[id]/page.tsx` must send the real current admin UUID to `PATCH /api/admin/conversations/:id/assignment`; the backend no longer interprets sentinel values like `'me'`.
- **Public message write contract:** Public `POST /conversations/:conversationId/messages` is now intentionally disabled; public user turns must go through `/api/chat` so message persistence, retrieval logs, status flow, and quota checks stay centralized.
- **Sidecar shared token:** Vector sidecar endpoints now expect `SIDECAR_SHARED_TOKEN` via `X-Sidecar-Token`; backend clients read `RAG_SIDECAR_SHARED_TOKEN || SIDECAR_SHARED_TOKEN` and docker compose binds the sidecar to localhost by default.
- **ChatService split:** `apps/api/src/chat/chat.service.ts` is no longer the only implementation surface; turn preparation lives in `chat-turn-context.service.ts`, and retrieval logging / unknown-question recording / assistant persistence live in `chat-turn-audit.service.ts`.
- **Admin conversation action route:** Conversation detail actions now converge on `admin/conversations/:id/(status|assignment|reply)`; `admin/workspace` remains for compatibility, but new admin UI work should prefer `admin/conversations`.
- **Sprint 2 test infra:** Local test infra now runs through `pnpm test:infra:up/down/restart` backed by `docker-compose.test.yml` with Postgres `5434`, Redis `6381`, and Python sidecar `8002`; `pnpm validate:release` orchestrates infra up -> schema push -> reset -> seed -> build -> validate -> RAG eval -> sidecar live -> cleanup.
- **Test seed compatibility:** Demo fixtures still use legacy short ids like `t-ecommerce-001` while Prisma models require UUIDs. `tests/scripts/seed-test-data.ts` must map fixture ids to stable UUIDs before writing relational data.
- **Root test script dependency resolution:** In this monorepo, root test scripts cannot reliably resolve `@prisma/client` / `bcryptjs`; DB-touching test scripts should run via `packages/database` and, when necessary, resolve dependencies from that package scope.
- **Settings integrations shared layer:** Admin integration pages should reuse `apps/web/src/app/admin/settings/integrations/shared.ts` for `XhsAccount` / `JuguangAccount` contracts, Juguang form mapping, webhook URL building, and shared account CRUD helpers to avoid drift between `/admin/settings`, `/admin/settings/xiaohongshu`, and `/admin/settings/juguang`.
- **Sidecar capability boundary:** Python sidecar `/health` now needs to be treated as the source of truth for parser/graph/vector capability state. It exposes `parser_mode`, `graph_mode`, `capabilities`, and nested `vector` health; TypeScript clients should normalize both snake_case and camelCase fields.
- **Workspace tenant-isolation tests:** `WorkspaceService` assignment/status mutations now use `prisma.conversation.updateMany` with `{ id, tenantId }`. Any unit harness for workspace mutation tests must mock `updateMany`, not `update`.
- **Post-Sprint 3 release verification:** `pnpm validate:release` still passes after the Sprint 3 governance changes. In this workspace, release validation may run RAG eval in fixture fallback mode when no live API is available, while sidecar live smoke should still pass against the test sidecar on `127.0.0.1:8002`.
- **Public chat session reset UX:** `apps/web/src/app/chat/[tenantSlug]/page.tsx` now uses “清除记录/clear history” to drop `conversationId`, `conversationToken`, lead-form state, and `localStorage` session cache, then bumps `retryNonce` to create a brand-new public conversation so old AI context is not reused.
- **Public chat return entry:** The public chat page now exposes a top-level “返回控制台” action that `prefetch`es and routes to `/admin/dashboard`, matching the premium button style used elsewhere in Web UI.
- **Analytics module contract:** `apps/api/src/analytics/analytics.service.ts` now normalizes dashboard, trend, intent distribution, platform comparison, after-sale, AI accuracy, and agent performance responses for the redesigned analytics page. Trend data includes `userMessages`, `assistantMessages`, `totalMessages`, `conversations`, `leads`, and `correctedReplies`; dashboard summary includes current-period totals, today totals, deltas, avg messages per conversation, lead capture rate, AI accuracy rate, and correction rate.
- **Analytics UI implementation:** `apps/web/src/app/admin/analytics/page.tsx` now implements the premium analytics experience without third-party chart libraries, using Tailwind + SVG/HTML for KPI cards, trend chart, donut distribution, ranking panels, and click-to-drilldown insight cards.

## Do-Not-Repeat
- After a production `pnpm --filter @ai-assistant/web build`, restart `next dev -p 3100` before Browser visual verification; otherwise stale webpack state can show `__webpack_modules__[moduleId] is not a function` even though the fresh build passes.
<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->

- [2026-06-08] In PowerShell, Next.js route paths containing brackets like `apps\web\src\app\chat\[tenantSlug]\page.tsx` must be read with `Get-Content -LiteralPath`; plain `-Path` treats brackets as wildcards.
- [2026-06-08] On Windows, do not pass recursive glob paths like `apps\api\src\*\*.service.ts` directly to `rg`; search the root path and constrain with `-g "*.service.ts"` / `-g "*.controller.ts"`.
- [2026-06-12] On Windows/WSL, `bash -lc "openwolf designqc ..."` can resolve the npm OpenWolf shim but fail with `node: not found`; run `openwolf designqc` directly from PowerShell in this workspace.
- [2026-06-09] In this Nest test/runtime setup, prefer `new HttpException(message, HttpStatus.TOO_MANY_REQUESTS)` over `new TooManyRequestsException(...)`; the latter caused a constructor/runtime failure during quota TDD.
- [2026-06-09] Do not point authenticated RAG eval calls at `/api/knowledge`; the guarded controller lives at `/api/admin/knowledge`, and using the old path can masquerade as a login failure.
- [2026-06-09] Do not trust localhost:4000 blindly for this workspace; check `netstat -ano | findstr :4000` and process command line before diagnosing API auth failures.
- [2026-06-09] This workspace's PowerShell does not support `Invoke-WebRequest -SkipHttpErrorCheck`; use Node fetch or `curl.exe` for compact HTTP diagnostics.
- [2026-06-09] `.wolf/buglog.json` currently has a UTF-8 BOM; strip `^\uFEFF` before validating it with raw Node `JSON.parse`.

- [2026-06-12] Do not trust `openwolf designqc` auto-detection in this workspace; pass `--url http://localhost:3100` or it can capture the unrelated app on localhost:3000.
- [2026-06-12] On Windows, `Start-Process -FilePath pnpm` can fail with "%1 is not a valid Win32 application"; use `pnpm.cmd` or `cmd.exe /c pnpm ...` for background dev server starts.
- [2026-06-14] Do not assume successful admin model-config tests mean public chat uses those credentials; verify `/api/chat` live because the old runtime path ignored tenant `modelConfig` and used env-only providers.
- [2026-06-14] After rebuilding/restarting API, re-check Docker Desktop/Postgres/Redis and restart Web dev on 3100 before claiming the browser chat flow works; API and Web may both be down even when code tests pass.
- [2026-06-15] In this environment, do not assume planning skill templates live under `.claude/skills`; `planning-with-files-zh` is installed under `.agents/skills`, so template/script reads must use that path.
- [2026-06-15] On this Windows workspace, `prisma generate` can fail with EPERM on `query_engine-windows.dll.node`; if regeneration is required, retry in an unlocked environment after clearing the `.prisma/client` directory.
- [2026-06-15] `docker compose` can fail with `project name must not be empty` from this Chinese-path workspace; set `COMPOSE_PROJECT_NAME` explicitly in scripted test infra instead of relying on Compose's inferred default.
- [2026-06-15] Release validation exports sidecar env vars into the process; sidecar-related unit tests must set both `SIDECAR_SHARED_TOKEN` and `RAG_SIDECAR_SHARED_TOKEN` explicitly to avoid environment leakage from surrounding commands.
- [2026-06-15] After converting workspace mutations to tenant-scoped `updateMany`, update `tests/unit/workspace-tenant-isolation.test.ts` mocks and assertions immediately; otherwise `pnpm validate` fails even though the service implementation is correct.
- [2026-06-15] When admin dashboard shows `ApiError: Internal server error` but only conversations/leads endpoints fail, check `pnpm exec prisma migrate status --schema prisma/schema.prisma` in `packages/database` before editing API code; this workspace hit a missing `platform_conversation_keys` migration rather than a service bug.
- [2026-06-15] If Next.js throws `Cannot find module './NNN.js'` from `apps/web/.next/server/webpack-runtime.js` while the same chunk exists under `.next/server/chunks/`, treat it as stale/corrupted `.next` output. Stop the 3100 web process, delete `apps/web/.next`, and restart `pnpm --filter @ai-assistant/web dev` instead of editing route code.

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->

- **Production hardening pass:** Public widget traffic must be token-gated with hashed conversation tokens, public polling should be incremental, ChatService should enforce quota before writes, Doudian product sync should upsert by tenant/platform/external ID, and RAG evaluation should prefer live pipeline checks when API dependencies are available.
- **Doudian sync:** DoudianService.syncProducts must call ProductsService.syncProductKnowledge after product upsert; direct Prisma writes bypass product knowledge/RAG sync.
- **Production readiness:** User confirmed the next target is production launch, so optimization work should prioritize security, tenant isolation, reliable jobs, and deployable configuration over demo-only convenience.
- **RAG sidecar:** User confirmed Python sidecar/RAG-Anything should be enabled now, not left as optional future work.
- **Admin Web auth:** User clarified the target client is the backend/admin Web app. Prefer httpOnly cookie authentication for admin Web instead of localStorage Bearer token; keep Bearer token only for non-browser/open API clients if needed.
- **turbovec rollout:** Implemented as an optional sidecar accelerator with `knowledge_chunks.vector_index_id` for stable uint64 IDs, not as a Postgres replacement. Keep default provider as pgvector until production chunk scale and latency targets justify enabling turbovec.
- **Production launch action items:** Completed the requested implementation pass for admin httpOnly cookie auth, tenant isolation, RAG sidecar parsing, queued knowledge indexing, Chinese retrieval tokenization, SQL-safe retrieval filters, deployable sidecar compose config, and executable verification scripts.
- **Customer service completeness pass:** Completed the handoff/review queue loop with tenant-scoped unknown-question resolution, final-status retrieval logs, admin conversation filters/actions, tenant-scoped admin human replies, chat client polling for persisted replies, and chat client use of backend `requiresHuman`/`answerStatus`/`conversationStatus`.
- **Full Web UI redesign:** Completed a premium SaaS visual pass across all Web routes: home, login, chat, admin layout, dashboard, conversations, conversation detail, leads, knowledge sources, chunks, retrieval test, products, orders, unknown questions, and settings. The change also fixed corrupted Chinese `zh` locale strings and locale names.
- **Meiqia-style optimization pass:** Completed a focused pass for tenant-aware AI customer-service runtime checks, workspace mutation isolation, platform reply text normalization, restored product/order/Doudian services for product-aware客服, PRP/spec/plan docs, and verification. Full unit suite, API build, and Web build passed.
- **Public chat AI/RAG fix:** Public chat now resolves tenant model config for LLM calls and embedding retrieval, handles pgvector dimension mismatch by falling back to keyword retrieval, and was live-verified through both API 4000 and Web proxy 3100 with knowledge-backed answers.
- **Sprint 2 delivery:** Chose explicit validation layering instead of overloading a single command: `validate:quick` for fast local regression, `validate` for default pre-merge checks, and `validate:release` for one-command infra-backed release validation with cleanup.
- **Sprint 3 governance pass:** Completed REST/error normalization, settings integration deduplication, and sidecar/RAG boundary clarification. Retrieval now logs semantic provider and fallback reason, while sidecar health explicitly reports parser/graph/vector capability state.
- **Analytics redesign:** Chose to keep the existing `/admin/analytics` route and high-level tabs, but standardized backend payloads first and then rebuilt the page with in-house SVG/HTML charts instead of adding a charting dependency. Interaction coverage now includes hover details, range/metric switching, and click-driven drilldown.
